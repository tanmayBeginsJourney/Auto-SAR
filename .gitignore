# Copied from .env.example; holds OPENAI_API_KEY and paths
.env
.env.*
!.env.example

# backend: running Python / installing deps locally
__pycache__/
*.py[cod]
.venv/
venv/

# frontend: npm install and vite build
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/

# Written at runtime by the FastAPI app (not source)
runtime_state/cases/
runtime_state/audit_ledger/
runtime_state/artifacts/
runtime_state/retrieval_index.json

# No package.json at repo root; npm may still drop this here
/package-lock.json
