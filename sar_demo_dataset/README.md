# Demo dataset

- `minimal_schema.sql` / `minimal_seed.sql` — schema and seed rows  
- `sar_demo_minimal.db` — bundled SQLite database for local runs  
- CSV sources — reference material used to build the minimal dataset  

Walkthroughs for the three linked demo cases: `demo_case_examples.md`.
